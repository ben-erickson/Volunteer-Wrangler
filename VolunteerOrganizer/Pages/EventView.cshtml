@page
@model VolunteerOrganizer.Pages.EventViewModel
@{
    ViewData["Title"] = "Event List";
}


<ul>
    @for (int i = 0; i < @Model.EventList.Count; i++)
    {
        <li>@Model.EventList[i].EventName</li>
    }
</ul>